#!/bin/sh
# Load a theme based on argument
unset block

ln -s ~/.config/bspwm/themes/${1}.bspwm_theme ~/.wm_theme
. ~/.wm_theme
rm ~/.config/termite/config
ln -s ~/.config/termite/themes/${TERMITE_CONFIG}.config ~/.config/termite/config

killall -s USR1 termite &
